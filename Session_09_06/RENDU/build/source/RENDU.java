/* autogenerated by Processing revision 1283 on 2022-06-09 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import processing.pdf.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class RENDU extends PApplet {

// Description : Affiche en Creative Coding M1 UX UI
// Author : Julien VAVRILLE

// Mes déclarations
String title = "Title goes here";
PFont titre;
PGraphics pg;
int text_size = 80;

boolean print_is = false;
//Fin déclarations

// SETUP
 public void setup() {
  /* size commented out by preprocessor */;
  background(0);
  noStroke();
}
// FIN SETUP


// BOUCLE DRAW
 public void draw() {
  String nom_de_fichier = "JuVAV_affiche"+year()+"_"+month()+"_"+day()+"_"+hour()+"_"+minute()+".pdf";
  if(print_is == true) {
    beginRecord(PDF,nom_de_fichier);
  }
  // blurshape();
  affichage_texte();
  affichage_ellipse();
  if(print_is == true) {
    endRecord();
    print_is = false;
  }
}
// FIN BOUCLE DRAW


// METHODES
 public void affichage_texte() {
  titre = createFont("AndoLight.ttf", text_size);
  // titre = createFont("Reznik-Light.ttf", text_size);
  fill(255);
  textFont(titre);
  textAlign(CENTER);
  text(title, width/2, 180);
}

 public void affichage_ellipse() {
  int x = width/2;
  int y = height/2;
  int taille = 200;
  fill(255,7,116);
  circle(x,y,taille);
}

 public void blurshape() {
  //pg = createGraphics(width, height, JAVA2D);
  //beginDraw();
  smooth();
  // background(0,0,0,0);
  noStroke();
  fill(255,0,0);
  circle(100,100,95);
  filter( BLUR, 6 );
  fill(255,255,0);
  circle(100,100,90);
  // filter( BLUR, 6 );
  circle(width/2, height/2, 300);
}

 public void keyPressed() {
  if(key == 'p') {
    print_is = true;
  }
}
// FIN METHODE


  public void settings() { size(563, 1000); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "RENDU" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
